package {{packageName}}.model;

import com.fasterxml.jackson.annotation.JsonFormat;
import com.fasterxml.jackson.annotation.JsonProperty;
import lombok.NoArgsConstructor;
import lombok.Data;
{{#if validation}}import javax.validation.constraints.*;{{/if}}

@Data
@NoArgsConstructor
public class {{pojoName}} {
  {{#columns}}
  {{#if underscore}}@JsonProperty("{{lower name}}"){{/if}}
  {{javaSig this}}
  {{/columns}}

  {{#if keys}}
  public String getId() {
    return String.format("{{#keys}}%s{{#unless @last}}-{{/unless}}{{/keys}}",
        {{#keys}}{{toJavaName key}}{{#unless @last}}, {{/unless}}{{/keys}}
    );
  }
  {{/if}}
}

